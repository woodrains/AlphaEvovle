# Current Program Performance
- Fitness Score: {fitness_score}
- Performance Metrics: {metrics}
- Feature Space: {feature_coords}

# Performance Analysis
{improvement_areas}

{artifacts}

# Evolution Context
{evolution_history}

# Current Program Code
```{language}
{current_program}
```

# Your Task: Improve EDP through Parameter Optimization

**CRITICAL REQUIREMENT**: You MUST modify the numeric return values in the decide_* functions.

**What to Change**:
1. Temporal tile sizes in decide_temporal_tiles() 
   - Focus on GlobalBuffer level (level_name == "GlobalBuffer")
   - Current values are likely too conservative
   - Try increasing K, P, Q while respecting buffer capacity

2. Spatial tiles in decide_spatial_tiles()
   - Current baseline uses NO spatial parallelism (all return 1)
   - DummyBuffer supports 14 PEs, GlobalBuffer supports 12 PEs
   - Try P_spatial=2, Q_spatial=7 or similar combinations

   **CRITICAL CONSTRAINT**: When adding spatial parallelism, you MUST adjust DRAM temporal tiles!
   Example (WRONG - violates dimension budget):
     GlobalBuffer temporal: P=14, Q=16
     DummyBuffer spatial: P=2, Q=7
     → P_total = 14×2 = 28, but we also need DRAM's P to multiply to 224
     → This will cause constraint violations!

   Example (CORRECT):
     GlobalBuffer temporal: P=14, Q=16
     DummyBuffer spatial: P=1, Q=1 (keep simple for now)
     DRAM temporal: P=16, Q=14 (completes the budget: 14×16=224, 16×14=224)
     → All dimension budgets satisfied ✓

   **SAFE APPROACH**: Start by only modifying temporal tiles. Add spatial parallelism later.

3. Dimension orders in decide_dimension_order()
   - Different levels need different orders for optimal reuse
   - Inner dimensions get more reuse

4. Bypass strategies in decide_bypass_strategy()
   - Current baseline bypasses nothing (conservative)
   - Try bypassing ["Weights"] at GlobalBuffer to save energy

**What NOT to Change**:
- Do NOT just modify comments or variable names
- Do NOT change code structure without changing parameter values
- Do NOT modify generate_mapping_decisions() function (outside EVOLVE-BLOCK)

**Performance Feedback Integration**:
Based on the metrics above:
- If combined_score > 0: You're improving! Try similar but larger changes
- If combined_score = 0: No change detected - you likely didn't modify return values
- If combined_score < 0: Try different dimensions or smaller tiles

**Systematic Exploration Example**:
If current GlobalBuffer returns {{'N': 1, 'K': 4, 'C': 1, 'P': 7, 'Q': 8, 'R': 1, 'S': 1}}
Try increasing one dimension: {{'N': 1, 'K': 8, 'C': 1, 'P': 7, 'Q': 8, 'R': 1, 'S': 1}}
Or try different dimension: {{'N': 1, 'K': 4, 'C': 1, 'P': 14, 'Q': 8, 'R': 1, 'S': 1}}
Or combine changes: {{'N': 1, 'K': 8, 'C': 1, 'P': 14, 'Q': 8, 'R': 1, 'S': 1}}

**SEARCH/REPLACE Format** (MANDATORY):

<<<<<<< SEARCH
# Exact code from current program (must match exactly, including indentation)
=======
# New code with DIFFERENT numeric return values
>>>>>>> REPLACE

You can make multiple SEARCH/REPLACE blocks in one response.

**IMPORTANT**: 
- Each SEARCH block must match EXACTLY (character-for-character)
- Focus on functions inside EVOLVE-BLOCK
- EXPLAIN your reasoning: "Increasing K from 4 to 8 should improve weight reuse..."
- Target 1-3 focused changes per iteration (not massive rewrites)
