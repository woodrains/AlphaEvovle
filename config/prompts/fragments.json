{
  "fitness_improved": "✅ EDP IMPROVED: Previous EDP={prev:.2e}, Current EDP={current:.2e}. Continue exploring similar parameter changes!",
  "fitness_declined": "⚠️ EDP INCREASED: Previous EDP={prev:.2e}, Current EDP={current:.2e}. Try different dimensions or smaller tiles.",
  "fitness_stable": "⚠️ NO CHANGE DETECTED: EDP={current:.2e}. This likely means you didn't modify numeric return values - MUST change parameters!",
  "exploring_region": "Exploring solution space at {features}",
  "code_too_long": "Code length ({threshold} chars) - consider simplifying",
  "no_specific_guidance": "**CRITICAL**: Change numeric return values in decide_temporal_tiles(), decide_spatial_tiles(). Focus on GlobalBuffer level. IMPORTANT: When modifying tiles, ensure dimension budget is satisfied (Π(all levels' tiles) = original dimension).",
  "parameter_unchanged_warning": "WARNING: Detected identical return values. You MUST modify numeric parameters to improve EDP!",
  "constraint_violation_hint": "⚠️ CONSTRAINT VIOLATION detected. Common causes:\n1. Dimension budget mismatch: Check that Π(temporal×spatial) across all levels equals original dimension\n2. Buffer capacity exceeded: Reduce tile sizes to fit buffer\n3. Spatial constraint exceeded: Reduce spatial parallelism to fit PE count\nSAFEST FIX: Only modify GlobalBuffer temporal tiles (K, P, Q), keep spatial tiles = 1, adjust DRAM tiles accordingly.",
  "baseline_comparison": "Baseline EDP: {baseline_edp:.2e}. Target: Beat baseline by >5%",
  "dimension_budget_error": "❌ DIMENSION BUDGET ERROR: When you change temporal OR spatial tiles at any level, you MUST update DRAM tiles to maintain: Π(all_levels) = original_dimension. See constraint violation details in artifacts."
}
